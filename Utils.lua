local Utils = {}

Utils.Math = require(script.MathUtils)
Utils.Vector = require(script.VectorUtils)
Utils.Instance = require(script.InstanceUtils)
Utils.Table = require(script.TableUtils)
Utils.Network = require(script.NetworkUtils)
Utils.UI = require(script.UIUtils)
Utils.Pathfinding = require(script.PathfindingUtils)
Utils.Debug = require(script.DebugUtils)

return Utils
